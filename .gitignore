# PlatformIO build cache (never commit)
.pio/
.pioenvs/
.piolibdeps/

# Secrets — fill these from the *.example.h templates (never commit)
# config.h holds your real Wi-Fi password and API key; only config.example.h is tracked.
config.h
**/config.h
secrets.h
**/secrets.h
tls_cert.h
**/tls_cert.h

# Build artifacts
*.bin
*.elf
*.hex
*.map

# Editor / OS
.vscode/
.DS_Store
Thumbs.db
desktop.ini
*.swp
